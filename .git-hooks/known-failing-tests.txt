# Tests that are EXPECTED to fail. The pre-push hook allows pushes when the
# failure set exactly matches this list. New failures or fixed-but-still-listed
# failures both block the push.
#
# Format: one test identifier per line, exact match against XCTest output.
# Comments start with #. Blank lines ignored.
#
# When you fix a test, REMOVE its entry here in the same commit.
# When you intentionally add a new failing test (Phase 2.7 / 3.2 style),
# ADD its entry here in the same commit with a comment citing the phase / reason.

# 0.1.0 — RealAudioChordTests fixed by the Basic Pitch + note-native consolidation.
# The chroma+template ChordDetector single-chord accuracy (GADA 40.6% / TaylorNylon 31.2% root) was
# the reason these were allowlisted. The Basic Pitch note-native path scores GADA 100% root / 93.8%
# exact and TaylorNylon 99.1% root / 92.7% exact (canonical pitch-class metric), comfortably above the
# 95%/80% root + 90%/65% exact thresholds, so both tests now pass and were removed from this list.

# Phase 3.2 — GuitarSet measurements (chord progression CSR below threshold)
# Ground truth (0.1.0, Basic Pitch + note-native path): Mean CSR 28.7% across the GuitarSet subset
# (was 37.5% on the removed chroma+template path). GuitarSet is full polyphonic comping with chord
# changes over time; the windowed note-native segmentation tracks it worse than single sustained chords.
# Threshold held at 50% to keep surfacing the progression-segmentation limitation (do not lower).
MusicCraftCoreTests.GuitarSetProgressionTests/testGuitarSetProgressionAccuracy

# Phase 3.3 — GuitarSet tempo:
#   - Buffer path: 1/3-tempo bug from RMS-based onset detection. Fixed in 0.0.11
#     by replacing the onset detector with Dixon 2006 spectral flux
#     (Sources/MusicCraftCore/DSP/SpectralFluxOnsetDetector.swift).
#   - JAMS-fed path: half-tempo harmonic outranking the base candidate due to
#     confidence=regularity*(1/ratio) weighting. Fixed in 0.0.11 by applying a
#     fixed 0.5 octave-error penalty to harmonics (TempoEstimator.swift).
# testGuitarSetTempoAccuracy is now passing at 100% within ±5% on the 5-fixture
# subset (was 0% before 0.0.11). Removed from the allowlist with the 0.0.11 work.

# Phase 3.2 — GuitarSet key inference (inherits chord accuracy)
# Ground truth (0.1.0, Basic Pitch + note-native path): 0% exact match, 20% relative match —
# unchanged from the removed chroma+template path; cascades from the GuitarSet progression accuracy above.
# Per Phase 3.2: measurements from chord-rich comping only (chord-based key inference path).
# MelodyKeyInference pitch-class fallback NOT exercised by these fixtures; do not claim general key accuracy.
MusicCraftCoreTests.GuitarSetKeyInferenceTests/testGuitarSetKeyInferenceAccuracy
